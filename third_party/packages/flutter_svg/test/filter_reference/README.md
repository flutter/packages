# Filter widget references

These SVG and PNG pairs are copied from the vector_graphics browser-reference
fixtures. See that package's `tool/filter_goldens.mjs` for rendering
parameters, independent references, and regeneration instructions. Widget tests
exercise both rendering strategies and string/memory loaders.
