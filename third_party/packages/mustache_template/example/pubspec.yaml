name: mustache_template_example

publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mustache_template:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
