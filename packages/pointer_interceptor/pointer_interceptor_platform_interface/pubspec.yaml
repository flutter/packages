name: pointer_interceptor_platform_interface
description: A common platform interface for the pointer_interceptor plugin.
repository: https://github.com/flutter/packages/tree/main/packages/pointer_inteceptor/pointer_inteceptor_platform_interface
issue_tracker: https://github.com/flutter/flutter/issues?q=is%3Aissue+is%3Aopen++label%3A%22p%3A+pointer_interceptor%22
# NOTE: We strongly prefer non-breaking changes, even at the expense of a
# less-clean API. See https://flutter.dev/go/platform-interface-breaking-changes
version: 1.0,0
homepage:

environment:
  sdk: '>=3.2.0-93.0.dev <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


