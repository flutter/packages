// Copyright 2013 The Flutter Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  @preconcurrency import Flutter
#elseif os(macOS)
  @preconcurrency import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class EventChannelWithoutClassesTestsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "EventChannelWithoutClassesTestsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>"))"
  }
}

enum EventChannelWithoutClassesTestsPigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

private class EventChannelWithoutClassesTestsPigeonCodecReader: FlutterStandardReader {
}

private class EventChannelWithoutClassesTestsPigeonCodecWriter: FlutterStandardWriter {
}

private class EventChannelWithoutClassesTestsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return EventChannelWithoutClassesTestsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return EventChannelWithoutClassesTestsPigeonCodecWriter(data: data)
  }
}

class EventChannelWithoutClassesTestsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = EventChannelWithoutClassesTestsPigeonCodec(
    readerWriter: EventChannelWithoutClassesTestsPigeonCodecReaderWriter())
}

let eventChannelWithoutClassesTestsPigeonMethodCodec = FlutterStandardMethodCodec(
  readerWriter: EventChannelWithoutClassesTestsPigeonCodecReaderWriter())

@MainActor
private class PigeonStreamHandler<ReturnType>: NSObject, @preconcurrency FlutterStreamHandler {
  private let wrapper: PigeonEventChannelWrapper<ReturnType>
  private var pigeonSink: PigeonEventSink<ReturnType>? = nil

  init(wrapper: PigeonEventChannelWrapper<ReturnType>) {
    self.wrapper = wrapper
  }

  func onListen(withArguments arguments: Any?, eventSink events: @escaping FlutterEventSink)
    -> FlutterError?
  {
    pigeonSink = PigeonEventSink<ReturnType>(events)
    wrapper.onListen(withArguments: arguments, sink: pigeonSink!)
    return nil
  }

  func onCancel(withArguments arguments: Any?) -> FlutterError? {
    pigeonSink = nil
    wrapper.onCancel(withArguments: arguments)
    return nil
  }
}
@MainActor
class StreamIntsAgainStreamHandler: PigeonEventChannelWrapper<Int64> {
  static func register(
    with messenger: FlutterBinaryMessenger,
    instanceName: String = "",
    streamHandler: StreamIntsAgainStreamHandler
  ) {
    var channelName =
      "dev.flutter.pigeon.pigeon_integration_tests.EventChannelMethods.streamIntsAgain"
    if !instanceName.isEmpty {
      channelName += ".\(instanceName)"
    }
    let internalStreamHandler = PigeonStreamHandler<Int64>(wrapper: streamHandler)
    let channel = FlutterEventChannel(
      name: channelName, binaryMessenger: messenger,
      codec: eventChannelWithoutClassesTestsPigeonMethodCodec)
    channel.setStreamHandler(internalStreamHandler)
  }
}
