// Copyright 2013 The Flutter Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  @preconcurrency import Flutter
#elseif os(macOS)
  @preconcurrency import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class NonNullFieldsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "NonNullFieldsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>"))"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? NonNullFieldsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> NonNullFieldsError {
  return NonNullFieldsError(
    code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.",
    details: "")
}

enum NonNullFieldsPigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
  static func doubleEquals(_ lhs: Double, _ rhs: Double) -> Bool {
    return (lhs.isNaN && rhs.isNaN) || lhs == rhs
  }

  static func doubleHash(_ value: Double, _ hasher: inout Hasher) {
    if value.isNaN {
      hasher.combine(0x7FF8_0000_0000_0000)
    } else {
      // Normalize -0.0 to 0.0
      hasher.combine(value == 0 ? 0 : value)
    }
  }

  static func deepEquals(_ lhs: Any?, _ rhs: Any?) -> Bool {
    let cleanLhs = nilOrValue(lhs) as Any?
    let cleanRhs = nilOrValue(rhs) as Any?
    switch (cleanLhs, cleanRhs) {
    case (nil, nil):
      return true

    case (nil, _), (_, nil):
      return false

    case (let lhs as AnyObject, let rhs as AnyObject) where lhs === rhs:
      return true

    case is (Void, Void):
      return true

    case (let lhsArray, let rhsArray) as ([Double], [Double]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !doubleEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsArray, let rhsArray) as ([Any?], [Any?]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !deepEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsDictionary, let rhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
      guard lhsDictionary.count == rhsDictionary.count else { return false }
      for (lhsKey, lhsValue) in lhsDictionary {
        var found = false
        for (rhsKey, rhsValue) in rhsDictionary {
          if deepEquals(lhsKey, rhsKey) {
            if deepEquals(lhsValue, rhsValue) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if !found { return false }
      }
      return true

    case (let lhs as Double, let rhs as Double):
      return doubleEquals(lhs, rhs)

    case (let lhsHashable, let rhsHashable) as (AnyHashable, AnyHashable):
      return lhsHashable == rhsHashable

    default:
      return false
    }
  }

  static func deepHash(value: Any?, hasher: inout Hasher) {
    let cleanValue = nilOrValue(value) as Any?
    if let cleanValue = cleanValue {
      if let doubleValue = cleanValue as? Double {
        doubleHash(doubleValue, &hasher)
      } else if let valueList = cleanValue as? [Any?] {
        for item in valueList {
          deepHash(value: item, hasher: &hasher)
        }
      } else if let valueList = cleanValue as? [Double] {
        for item in valueList {
          doubleHash(item, &hasher)
        }
      } else if let valueDict = cleanValue as? [AnyHashable: Any?] {
        var result = 0
        for (key, value) in valueDict {
          var entryKeyHasher = Hasher()
          deepHash(value: key, hasher: &entryKeyHasher)
          var entryValueHasher = Hasher()
          deepHash(value: value, hasher: &entryValueHasher)
          result = result &+ ((entryKeyHasher.finalize() &* 31) ^ entryValueHasher.finalize())
        }
        hasher.combine(result)
      } else if let hashableValue = cleanValue as? AnyHashable {
        hasher.combine(hashableValue)
      } else {
        hasher.combine(String(describing: cleanValue))
      }
    } else {
      hasher.combine(0)
    }
  }

}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

enum ReplyType: Int, CaseIterable {
  case success = 0
  case error = 1
}

/// Generated class from Pigeon that represents data sent in messages.
struct NonNullFieldSearchRequest: Hashable, CustomStringConvertible {
  var query: String

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> NonNullFieldSearchRequest? {
    let query = pigeonVar_list[0] as! String

    return NonNullFieldSearchRequest(
      query: query
    )
  }
  func toList() -> [Any?] {
    return [
      query
    ]
  }
  static func == (lhs: NonNullFieldSearchRequest, rhs: NonNullFieldSearchRequest) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return NonNullFieldsPigeonInternal.deepEquals(lhs.query, rhs.query)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("NonNullFieldSearchRequest")
    NonNullFieldsPigeonInternal.deepHash(value: query, hasher: &hasher)
  }

  public var description: String {
    return "NonNullFieldSearchRequest(query: \(String(describing: query)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct ExtraData: Hashable, CustomStringConvertible {
  var detailA: String
  var detailB: String

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> ExtraData? {
    let detailA = pigeonVar_list[0] as! String
    let detailB = pigeonVar_list[1] as! String

    return ExtraData(
      detailA: detailA,
      detailB: detailB
    )
  }
  func toList() -> [Any?] {
    return [
      detailA,
      detailB,
    ]
  }
  static func == (lhs: ExtraData, rhs: ExtraData) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return NonNullFieldsPigeonInternal.deepEquals(lhs.detailA, rhs.detailA)
      && NonNullFieldsPigeonInternal.deepEquals(lhs.detailB, rhs.detailB)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("ExtraData")
    NonNullFieldsPigeonInternal.deepHash(value: detailA, hasher: &hasher)
    NonNullFieldsPigeonInternal.deepHash(value: detailB, hasher: &hasher)
  }

  public var description: String {
    return
      "ExtraData(detailA: \(String(describing: detailA)), detailB: \(String(describing: detailB)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct NonNullFieldSearchReply: Hashable, CustomStringConvertible {
  var result: String
  var error: String
  var indices: [Int64?]
  var extraData: ExtraData
  var type: ReplyType

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> NonNullFieldSearchReply? {
    let result = pigeonVar_list[0] as! String
    let error = pigeonVar_list[1] as! String
    let indices = pigeonVar_list[2] as! [Int64?]
    let extraData = pigeonVar_list[3] as! ExtraData
    let type = pigeonVar_list[4] as! ReplyType

    return NonNullFieldSearchReply(
      result: result,
      error: error,
      indices: indices,
      extraData: extraData,
      type: type
    )
  }
  func toList() -> [Any?] {
    return [
      result,
      error,
      indices,
      extraData,
      type,
    ]
  }
  static func == (lhs: NonNullFieldSearchReply, rhs: NonNullFieldSearchReply) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return NonNullFieldsPigeonInternal.deepEquals(lhs.result, rhs.result)
      && NonNullFieldsPigeonInternal.deepEquals(lhs.error, rhs.error)
      && NonNullFieldsPigeonInternal.deepEquals(lhs.indices, rhs.indices)
      && NonNullFieldsPigeonInternal.deepEquals(lhs.extraData, rhs.extraData)
      && NonNullFieldsPigeonInternal.deepEquals(lhs.type, rhs.type)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("NonNullFieldSearchReply")
    NonNullFieldsPigeonInternal.deepHash(value: result, hasher: &hasher)
    NonNullFieldsPigeonInternal.deepHash(value: error, hasher: &hasher)
    NonNullFieldsPigeonInternal.deepHash(value: indices, hasher: &hasher)
    NonNullFieldsPigeonInternal.deepHash(value: extraData, hasher: &hasher)
    NonNullFieldsPigeonInternal.deepHash(value: type, hasher: &hasher)
  }

  public var description: String {
    return
      "NonNullFieldSearchReply(result: \(String(describing: result)), error: \(String(describing: error)), indices: \(String(describing: indices)), extraData: \(String(describing: extraData)), type: \(String(describing: type)))"
  }
}

private class NonNullFieldsPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return ReplyType(rawValue: enumResultAsInt)
      }
      return nil
    case 130:
      return NonNullFieldSearchRequest.fromList(self.readValue() as! [Any?])
    case 131:
      return ExtraData.fromList(self.readValue() as! [Any?])
    case 132:
      return NonNullFieldSearchReply.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class NonNullFieldsPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? ReplyType {
      super.writeByte(129)
      super.writeValue(value.rawValue)
    } else if let value = value as? NonNullFieldSearchRequest {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else if let value = value as? ExtraData {
      super.writeByte(131)
      super.writeValue(value.toList())
    } else if let value = value as? NonNullFieldSearchReply {
      super.writeByte(132)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class NonNullFieldsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return NonNullFieldsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return NonNullFieldsPigeonCodecWriter(data: data)
  }
}

class NonNullFieldsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = NonNullFieldsPigeonCodec(readerWriter: NonNullFieldsPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NonNullFieldHostApi {
  @MainActor func search(nested: NonNullFieldSearchRequest) throws -> NonNullFieldSearchReply
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NonNullFieldHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NonNullFieldsPigeonCodec.shared }
  /// Sets up an instance of `NonNullFieldHostApi` to handle messages through the `binaryMessenger`.
  @MainActor static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NonNullFieldHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let searchChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.pigeon_integration_tests.NonNullFieldHostApi.search\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      @MainActor func handler(message: Any?, reply: @escaping @Sendable (Any?) -> Void) {
        let args = message as! [Any?]
        let nestedArg = args[0] as! NonNullFieldSearchRequest
        do {
          let result = try api.search(nested: nestedArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
      searchChannel.setMessageHandler(handler)
    } else {
      searchChannel.setMessageHandler(nil)
    }
  }
}

/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NonNullFieldFlutterApiProtocol {
  @MainActor func search(request requestArg: NonNullFieldSearchRequest) async throws
    -> NonNullFieldSearchReply
}
class NonNullFieldFlutterApi: NonNullFieldFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NonNullFieldsPigeonCodec {
    return NonNullFieldsPigeonCodec.shared
  }
  @MainActor func search(request requestArg: NonNullFieldSearchRequest) async throws
    -> NonNullFieldSearchReply
  {
    return try await withCheckedThrowingContinuation { continuation in
      let channelName: String =
        "dev.flutter.pigeon.pigeon_integration_tests.NonNullFieldFlutterApi.search\(messageChannelSuffix)"
      let channel = FlutterBasicMessageChannel(
        name: channelName, binaryMessenger: binaryMessenger, codec: codec)
      channel.sendMessage([requestArg] as [Any?]) { (response: any Sendable) in
        MainActor.assumeIsolated {
          guard let listResponse = response as? [Any?] else {
            continuation.resume(throwing: createConnectionError(withChannelName: channelName))
            return
          }
          if listResponse.count > 1 {
            let code: String = listResponse[0] as! String
            let message: String? = nilOrValue(listResponse[1])
            let details: String? = nilOrValue(listResponse[2])
            continuation.resume(
              throwing: NonNullFieldsError(code: code, message: message, details: details))
          } else if listResponse[0] == nil {
            continuation.resume(
              throwing: NonNullFieldsError(
                code: "null-error",
                message: "Flutter api returned null value for non-null return value.", details: ""))
          } else {
            let result = listResponse[0] as! NonNullFieldSearchReply
            continuation.resume(returning: result)
          }
        }
      }
    }
  }
}
