// Copyright 2013 The Flutter Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  @preconcurrency import Flutter
#elseif os(macOS)
  @preconcurrency import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class FlutterUnittestsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "FlutterUnittestsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>"))"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? FlutterUnittestsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

enum FlutterUnittestsPigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
  static func doubleEquals(_ lhs: Double, _ rhs: Double) -> Bool {
    return (lhs.isNaN && rhs.isNaN) || lhs == rhs
  }

  static func doubleHash(_ value: Double, _ hasher: inout Hasher) {
    if value.isNaN {
      hasher.combine(0x7FF8_0000_0000_0000)
    } else {
      // Normalize -0.0 to 0.0
      hasher.combine(value == 0 ? 0 : value)
    }
  }

  static func deepEquals(_ lhs: Any?, _ rhs: Any?) -> Bool {
    let cleanLhs = nilOrValue(lhs) as Any?
    let cleanRhs = nilOrValue(rhs) as Any?
    switch (cleanLhs, cleanRhs) {
    case (nil, nil):
      return true

    case (nil, _), (_, nil):
      return false

    case (let lhs as AnyObject, let rhs as AnyObject) where lhs === rhs:
      return true

    case is (Void, Void):
      return true

    case (let lhsArray, let rhsArray) as ([Double], [Double]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !doubleEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsArray, let rhsArray) as ([Any?], [Any?]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !deepEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsDictionary, let rhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
      guard lhsDictionary.count == rhsDictionary.count else { return false }
      for (lhsKey, lhsValue) in lhsDictionary {
        var found = false
        for (rhsKey, rhsValue) in rhsDictionary {
          if deepEquals(lhsKey, rhsKey) {
            if deepEquals(lhsValue, rhsValue) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if !found { return false }
      }
      return true

    case (let lhs as Double, let rhs as Double):
      return doubleEquals(lhs, rhs)

    case (let lhsHashable, let rhsHashable) as (AnyHashable, AnyHashable):
      return lhsHashable == rhsHashable

    default:
      return false
    }
  }

  static func deepHash(value: Any?, hasher: inout Hasher) {
    let cleanValue = nilOrValue(value) as Any?
    if let cleanValue = cleanValue {
      if let doubleValue = cleanValue as? Double {
        doubleHash(doubleValue, &hasher)
      } else if let valueList = cleanValue as? [Any?] {
        for item in valueList {
          deepHash(value: item, hasher: &hasher)
        }
      } else if let valueList = cleanValue as? [Double] {
        for item in valueList {
          doubleHash(item, &hasher)
        }
      } else if let valueDict = cleanValue as? [AnyHashable: Any?] {
        var result = 0
        for (key, value) in valueDict {
          var entryKeyHasher = Hasher()
          deepHash(value: key, hasher: &entryKeyHasher)
          var entryValueHasher = Hasher()
          deepHash(value: value, hasher: &entryValueHasher)
          result = result &+ ((entryKeyHasher.finalize() &* 31) ^ entryValueHasher.finalize())
        }
        hasher.combine(result)
      } else if let hashableValue = cleanValue as? AnyHashable {
        hasher.combine(hashableValue)
      } else {
        hasher.combine(String(describing: cleanValue))
      }
    } else {
      hasher.combine(0)
    }
  }

}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

/// Generated class from Pigeon that represents data sent in messages.
struct FlutterSearchRequest: Hashable, CustomStringConvertible {
  var query: String? = nil

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> FlutterSearchRequest? {
    let query: String? = nilOrValue(pigeonVar_list[0])

    return FlutterSearchRequest(
      query: query
    )
  }
  func toList() -> [Any?] {
    return [
      query
    ]
  }
  static func == (lhs: FlutterSearchRequest, rhs: FlutterSearchRequest) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return FlutterUnittestsPigeonInternal.deepEquals(lhs.query, rhs.query)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("FlutterSearchRequest")
    FlutterUnittestsPigeonInternal.deepHash(value: query, hasher: &hasher)
  }

  public var description: String {
    return "FlutterSearchRequest(query: \(String(describing: query)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct FlutterSearchReply: Hashable, CustomStringConvertible {
  var result: String? = nil
  var error: String? = nil

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> FlutterSearchReply? {
    let result: String? = nilOrValue(pigeonVar_list[0])
    let error: String? = nilOrValue(pigeonVar_list[1])

    return FlutterSearchReply(
      result: result,
      error: error
    )
  }
  func toList() -> [Any?] {
    return [
      result,
      error,
    ]
  }
  static func == (lhs: FlutterSearchReply, rhs: FlutterSearchReply) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return FlutterUnittestsPigeonInternal.deepEquals(lhs.result, rhs.result)
      && FlutterUnittestsPigeonInternal.deepEquals(lhs.error, rhs.error)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("FlutterSearchReply")
    FlutterUnittestsPigeonInternal.deepHash(value: result, hasher: &hasher)
    FlutterUnittestsPigeonInternal.deepHash(value: error, hasher: &hasher)
  }

  public var description: String {
    return
      "FlutterSearchReply(result: \(String(describing: result)), error: \(String(describing: error)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct FlutterSearchRequests: Hashable, CustomStringConvertible {
  var requests: [Any?]? = nil

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> FlutterSearchRequests? {
    let requests: [Any?]? = nilOrValue(pigeonVar_list[0])

    return FlutterSearchRequests(
      requests: requests
    )
  }
  func toList() -> [Any?] {
    return [
      requests
    ]
  }
  static func == (lhs: FlutterSearchRequests, rhs: FlutterSearchRequests) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return FlutterUnittestsPigeonInternal.deepEquals(lhs.requests, rhs.requests)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("FlutterSearchRequests")
    FlutterUnittestsPigeonInternal.deepHash(value: requests, hasher: &hasher)
  }

  public var description: String {
    return "FlutterSearchRequests(requests: \(String(describing: requests)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct FlutterSearchReplies: Hashable, CustomStringConvertible {
  var replies: [Any?]? = nil

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> FlutterSearchReplies? {
    let replies: [Any?]? = nilOrValue(pigeonVar_list[0])

    return FlutterSearchReplies(
      replies: replies
    )
  }
  func toList() -> [Any?] {
    return [
      replies
    ]
  }
  static func == (lhs: FlutterSearchReplies, rhs: FlutterSearchReplies) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return FlutterUnittestsPigeonInternal.deepEquals(lhs.replies, rhs.replies)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("FlutterSearchReplies")
    FlutterUnittestsPigeonInternal.deepHash(value: replies, hasher: &hasher)
  }

  public var description: String {
    return "FlutterSearchReplies(replies: \(String(describing: replies)))"
  }
}

private class FlutterUnittestsPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      return FlutterSearchRequest.fromList(self.readValue() as! [Any?])
    case 130:
      return FlutterSearchReply.fromList(self.readValue() as! [Any?])
    case 131:
      return FlutterSearchRequests.fromList(self.readValue() as! [Any?])
    case 132:
      return FlutterSearchReplies.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class FlutterUnittestsPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? FlutterSearchRequest {
      super.writeByte(129)
      super.writeValue(value.toList())
    } else if let value = value as? FlutterSearchReply {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else if let value = value as? FlutterSearchRequests {
      super.writeByte(131)
      super.writeValue(value.toList())
    } else if let value = value as? FlutterSearchReplies {
      super.writeByte(132)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class FlutterUnittestsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return FlutterUnittestsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return FlutterUnittestsPigeonCodecWriter(data: data)
  }
}

class FlutterUnittestsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = FlutterUnittestsPigeonCodec(
    readerWriter: FlutterUnittestsPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol Api {
  @MainActor func search(request: FlutterSearchRequest) throws -> FlutterSearchReply
  @MainActor func doSearches(request: FlutterSearchRequests) throws -> FlutterSearchReplies
  @MainActor func echo(requests: FlutterSearchRequests) throws -> FlutterSearchRequests
  @MainActor func anInt(value: Int64) throws -> Int64
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class ApiSetup {
  static var codec: FlutterStandardMessageCodec { FlutterUnittestsPigeonCodec.shared }
  /// Sets up an instance of `Api` to handle messages through the `binaryMessenger`.
  @MainActor static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: Api?, messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let searchChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.Api.search\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      @MainActor func handler(message: Any?, reply: @escaping @Sendable (Any?) -> Void) {
        let args = message as! [Any?]
        let requestArg = args[0] as! FlutterSearchRequest
        do {
          let result = try api.search(request: requestArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
      searchChannel.setMessageHandler(handler)
    } else {
      searchChannel.setMessageHandler(nil)
    }
    let doSearchesChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.Api.doSearches\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      @MainActor func handler(message: Any?, reply: @escaping @Sendable (Any?) -> Void) {
        let args = message as! [Any?]
        let requestArg = args[0] as! FlutterSearchRequests
        do {
          let result = try api.doSearches(request: requestArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
      doSearchesChannel.setMessageHandler(handler)
    } else {
      doSearchesChannel.setMessageHandler(nil)
    }
    let echoChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.Api.echo\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      @MainActor func handler(message: Any?, reply: @escaping @Sendable (Any?) -> Void) {
        let args = message as! [Any?]
        let requestsArg = args[0] as! FlutterSearchRequests
        do {
          let result = try api.echo(requests: requestsArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
      echoChannel.setMessageHandler(handler)
    } else {
      echoChannel.setMessageHandler(nil)
    }
    let anIntChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.Api.anInt\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      @MainActor func handler(message: Any?, reply: @escaping @Sendable (Any?) -> Void) {
        let args = message as! [Any?]
        let valueArg = args[0] as! Int64
        do {
          let result = try api.anInt(value: valueArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
      anIntChannel.setMessageHandler(handler)
    } else {
      anIntChannel.setMessageHandler(nil)
    }
  }
}
