// Copyright 2013 The Flutter Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  @preconcurrency import Flutter
#elseif os(macOS)
  @preconcurrency import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class NullFieldsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "NullFieldsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? NullFieldsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(Swift.type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> NullFieldsError {
  return NullFieldsError(
    code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.",
    details: "")
}

enum NullFieldsPigeonInternal {
  static func isNullish(_ value: Any?) -> Bool {
    guard let innerValue = value else {
      return true
    }

    if case Optional<Any>.some(Optional<Any>.none) = value {
      return true
    }

    return innerValue is NSNull
  }
  static func doubleEquals(_ lhs: Double, _ rhs: Double) -> Bool {
    return (lhs.isNaN && rhs.isNaN) || lhs == rhs
  }

  static func doubleHash(_ value: Double, _ hasher: inout Hasher) {
    if value.isNaN {
      hasher.combine(0x7FF8_0000_0000_0000)
    } else {
      // Normalize -0.0 to 0.0
      hasher.combine(value == 0 ? 0 : value)
    }
  }

  static func deepEquals(_ lhs: Any?, _ rhs: Any?) -> Bool {
    let cleanLhs = nilOrValue(lhs) as Any?
    let cleanRhs = nilOrValue(rhs) as Any?
    switch (cleanLhs, cleanRhs) {
    case (nil, nil):
      return true

    case (nil, _), (_, nil):
      return false

    case (let lhs as AnyObject, let rhs as AnyObject) where lhs === rhs:
      return true

    case is (Void, Void):
      return true

    case (let lhsArray, let rhsArray) as ([Any?], [Any?]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !deepEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsArray, let rhsArray) as ([Double], [Double]):
      guard lhsArray.count == rhsArray.count else { return false }
      for (index, element) in lhsArray.enumerated() {
        if !doubleEquals(element, rhsArray[index]) {
          return false
        }
      }
      return true

    case (let lhsDictionary, let rhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
      guard lhsDictionary.count == rhsDictionary.count else { return false }
      for (lhsKey, lhsValue) in lhsDictionary {
        var found = false
        for (rhsKey, rhsValue) in rhsDictionary {
          if deepEquals(lhsKey, rhsKey) {
            if deepEquals(lhsValue, rhsValue) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if !found { return false }
      }
      return true

    case (let lhs as Double, let rhs as Double):
      return doubleEquals(lhs, rhs)

    case (let lhsHashable, let rhsHashable) as (AnyHashable, AnyHashable):
      return lhsHashable == rhsHashable

    default:
      return false
    }
  }

  static func deepHash(value: Any?, hasher: inout Hasher) {
    let cleanValue = nilOrValue(value) as Any?
    if let cleanValue = cleanValue {
      if let doubleValue = cleanValue as? Double {
        doubleHash(doubleValue, &hasher)
      } else if let valueList = cleanValue as? [Any?] {
        for item in valueList {
          deepHash(value: item, hasher: &hasher)
        }
      } else if let valueList = cleanValue as? [Double] {
        for item in valueList {
          doubleHash(item, &hasher)
        }
      } else if let valueDict = cleanValue as? [AnyHashable: Any?] {
        var result = 0
        for (key, value) in valueDict {
          var entryKeyHasher = Hasher()
          deepHash(value: key, hasher: &entryKeyHasher)
          var entryValueHasher = Hasher()
          deepHash(value: value, hasher: &entryValueHasher)
          result = result &+ ((entryKeyHasher.finalize() &* 31) ^ entryValueHasher.finalize())
        }
        hasher.combine(result)
      } else if let hashableValue = cleanValue as? AnyHashable {
        hasher.combine(hashableValue)
      } else {
        hasher.combine(String(describing: cleanValue))
      }
    } else {
      hasher.combine(0)
    }
  }

}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

enum NullFieldsSearchReplyType: Int, CaseIterable {
  case success = 0
  case failure = 1
}

/// Generated class from Pigeon that represents data sent in messages.
struct NullFieldsSearchRequest: Hashable, CustomStringConvertible {
  var query: String? = nil
  var identifier: Int64

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> NullFieldsSearchRequest? {
    let query: String? = nilOrValue(pigeonVar_list[0])
    let identifier = pigeonVar_list[1] as! Int64

    return NullFieldsSearchRequest(
      query: query,
      identifier: identifier
    )
  }
  func toList() -> [Any?] {
    return [
      query,
      identifier,
    ]
  }
  static func == (lhs: NullFieldsSearchRequest, rhs: NullFieldsSearchRequest) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return NullFieldsPigeonInternal.deepEquals(lhs.query, rhs.query)
      && NullFieldsPigeonInternal.deepEquals(lhs.identifier, rhs.identifier)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("NullFieldsSearchRequest")
    NullFieldsPigeonInternal.deepHash(value: query, hasher: &hasher)
    NullFieldsPigeonInternal.deepHash(value: identifier, hasher: &hasher)
  }

  public var description: String {
    return
      "NullFieldsSearchRequest(query: \(String(describing: query)), identifier: \(String(describing: identifier)))"
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct NullFieldsSearchReply: Hashable, CustomStringConvertible {
  var result: String? = nil
  var error: String? = nil
  var indices: [Int64?]? = nil
  var request: NullFieldsSearchRequest? = nil
  var type: NullFieldsSearchReplyType? = nil

  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> NullFieldsSearchReply? {
    let result: String? = nilOrValue(pigeonVar_list[0])
    let error: String? = nilOrValue(pigeonVar_list[1])
    let indices: [Int64?]? = nilOrValue(pigeonVar_list[2])
    let request: NullFieldsSearchRequest? = nilOrValue(pigeonVar_list[3])
    let type: NullFieldsSearchReplyType? = nilOrValue(pigeonVar_list[4])

    return NullFieldsSearchReply(
      result: result,
      error: error,
      indices: indices,
      request: request,
      type: type
    )
  }
  func toList() -> [Any?] {
    return [
      result,
      error,
      indices,
      request,
      type,
    ]
  }
  static func == (lhs: NullFieldsSearchReply, rhs: NullFieldsSearchReply) -> Bool {
    if Swift.type(of: lhs) != Swift.type(of: rhs) {
      return false
    }
    return NullFieldsPigeonInternal.deepEquals(lhs.result, rhs.result)
      && NullFieldsPigeonInternal.deepEquals(lhs.error, rhs.error)
      && NullFieldsPigeonInternal.deepEquals(lhs.indices, rhs.indices)
      && NullFieldsPigeonInternal.deepEquals(lhs.request, rhs.request)
      && NullFieldsPigeonInternal.deepEquals(lhs.type, rhs.type)
  }

  func hash(into hasher: inout Hasher) {
    hasher.combine("NullFieldsSearchReply")
    NullFieldsPigeonInternal.deepHash(value: result, hasher: &hasher)
    NullFieldsPigeonInternal.deepHash(value: error, hasher: &hasher)
    NullFieldsPigeonInternal.deepHash(value: indices, hasher: &hasher)
    NullFieldsPigeonInternal.deepHash(value: request, hasher: &hasher)
    NullFieldsPigeonInternal.deepHash(value: type, hasher: &hasher)
  }

  public var description: String {
    return
      "NullFieldsSearchReply(result: \(String(describing: result)), error: \(String(describing: error)), indices: \(String(describing: indices)), request: \(String(describing: request)), type: \(String(describing: type)))"
  }
}

private class NullFieldsPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return NullFieldsSearchReplyType(rawValue: enumResultAsInt)
      }
      return nil
    case 130:
      return NullFieldsSearchRequest.fromList(self.readValue() as! [Any?])
    case 131:
      return NullFieldsSearchReply.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class NullFieldsPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? NullFieldsSearchReplyType {
      super.writeByte(129)
      super.writeValue(value.rawValue)
    } else if let value = value as? NullFieldsSearchRequest {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else if let value = value as? NullFieldsSearchReply {
      super.writeByte(131)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class NullFieldsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return NullFieldsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return NullFieldsPigeonCodecWriter(data: data)
  }
}

class NullFieldsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = NullFieldsPigeonCodec(readerWriter: NullFieldsPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullFieldsHostApi {
  @MainActor func search(nested: NullFieldsSearchRequest) throws -> NullFieldsSearchReply
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullFieldsHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullFieldsPigeonCodec.shared }
  /// Sets up an instance of `NullFieldsHostApi` to handle messages through the `binaryMessenger`.
  @MainActor static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullFieldsHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let searchChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.NullFieldsHostApi.search\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      searchChannel.setMessageHandler {
        @MainActor (message: Any?, reply: @escaping @Sendable (Any?) -> Void) in
        let args = message as! [Any?]
        let nestedArg = args[0] as! NullFieldsSearchRequest
        do {
          let result = try api.search(nested: nestedArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      searchChannel.setMessageHandler(nil)
    }
  }
}

/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullFieldsFlutterApiProtocol {
  @MainActor func search(request requestArg: NullFieldsSearchRequest) async throws
    -> NullFieldsSearchReply
}
class NullFieldsFlutterApi: NullFieldsFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullFieldsPigeonCodec {
    return NullFieldsPigeonCodec.shared
  }
  @MainActor func search(request requestArg: NullFieldsSearchRequest) async throws
    -> NullFieldsSearchReply
  {
    return try await withCheckedThrowingContinuation { continuation in
      let channelName: String =
        "dev.flutter.pigeon.pigeon_integration_tests.NullFieldsFlutterApi.search\(messageChannelSuffix)"
      let channel = FlutterBasicMessageChannel(
        name: channelName, binaryMessenger: binaryMessenger, codec: codec)
      channel.sendMessage([requestArg] as [Any?]) { (response: any Sendable) in
        MainActor.assumeIsolated {
          guard let listResponse = response as? [Any?] else {
            continuation.resume(throwing: createConnectionError(withChannelName: channelName))
            return
          }
          if listResponse.count > 1 {
            let code: String = listResponse[0] as! String
            let message: String? = nilOrValue(listResponse[1])
            let details: String? = nilOrValue(listResponse[2])
            continuation.resume(
              throwing: NullFieldsError(code: code, message: message, details: details))
          } else if listResponse[0] == nil {
            continuation.resume(
              throwing: NullFieldsError(
                code: "null-error",
                message: "Flutter api returned null value for non-null return value.", details: ""))
          } else {
            let result = listResponse[0] as! NullFieldsSearchReply
            continuation.resume(returning: result)
          }
        }
      }
    }
  }
}
