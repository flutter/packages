name: test_plugin
description: Pigeon test harness for primary plugin languages.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

flutter:
  plugin:
    platforms:
      android:
        package: com.example.test_plugin
        pluginClass: TestPlugin
      ios:
        pluginClass: TestPlugin
      linux:
        pluginClass: TestPlugin
      macos:
        pluginClass: TestPlugin
      windows:
        pluginClass: TestPluginCApi

dependencies:
  flutter:
    sdk: flutter
  jni: 
    git:
      url: https://github.com/dart-lang/native/
      path: pkgs/jni
      ref: 735250a19b1a429869bfacccba833ecc93db34b6

dev_dependencies:
  flutter_test:
    sdk: flutter
