// Copyright 2013 The Flutter Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon
// swift-format-ignore-file: AlwaysUseLowerCamelCase

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class NullFieldsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "NullFieldsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? NullFieldsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> NullFieldsError {
  return NullFieldsError(
    code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.",
    details: "")
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

enum NullFieldsSearchReplyType: Int {
  case success = 0
  case failure = 1
}

/// Generated class from Pigeon that represents data sent in messages.
struct NullFieldsSearchRequest {
  var query: String? = nil
  var identifier: Int64

  static func fromList(_ pigeonVar_list: [Any?]) -> NullFieldsSearchRequest? {
    let query: String? = nilOrValue(pigeonVar_list[0])
    let identifier = pigeonVar_list[1] as! Int64

    return NullFieldsSearchRequest(
      query: query,
      identifier: identifier
    )
  }
  func toList() -> [Any?] {
    return [
      query,
      identifier,
    ]
  }
}

/// Generated class from Pigeon that represents data sent in messages.
struct NullFieldsSearchReply {
  var result: String? = nil
  var error: String? = nil
  var indices: [Int64?]? = nil
  var request: NullFieldsSearchRequest? = nil
  var type: NullFieldsSearchReplyType? = nil

  static func fromList(_ pigeonVar_list: [Any?]) -> NullFieldsSearchReply? {
    let result: String? = nilOrValue(pigeonVar_list[0])
    let error: String? = nilOrValue(pigeonVar_list[1])
    let indices: [Int64?]? = nilOrValue(pigeonVar_list[2])
    let request: NullFieldsSearchRequest? = nilOrValue(pigeonVar_list[3])
    let type: NullFieldsSearchReplyType? = nilOrValue(pigeonVar_list[4])

    return NullFieldsSearchReply(
      result: result,
      error: error,
      indices: indices,
      request: request,
      type: type
    )
  }
  func toList() -> [Any?] {
    return [
      result,
      error,
      indices,
      request,
      type,
    ]
  }
}

private class NullFieldsPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return NullFieldsSearchReplyType(rawValue: enumResultAsInt)
      }
      return nil
    case 130:
      return NullFieldsSearchRequest.fromList(self.readValue() as! [Any?])
    case 131:
      return NullFieldsSearchReply.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class NullFieldsPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? NullFieldsSearchReplyType {
      super.writeByte(129)
      super.writeValue(value.rawValue)
    } else if let value = value as? NullFieldsSearchRequest {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else if let value = value as? NullFieldsSearchReply {
      super.writeByte(131)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class NullFieldsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return NullFieldsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return NullFieldsPigeonCodecWriter(data: data)
  }
}

class NullFieldsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = NullFieldsPigeonCodec(readerWriter: NullFieldsPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullFieldsHostApi {
  func search(nested: NullFieldsSearchRequest) throws -> NullFieldsSearchReply
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullFieldsHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullFieldsPigeonCodec.shared }
  /// Sets up an instance of `NullFieldsHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullFieldsHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let searchChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.NullFieldsHostApi.search\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      searchChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let nestedArg = args[0] as! NullFieldsSearchRequest
        do {
          let result = try api.search(nested: nestedArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      searchChannel.setMessageHandler(nil)
    }
  }
}
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullFieldsFlutterApiProtocol {
  func search(
    request requestArg: NullFieldsSearchRequest,
    completion: @escaping (Result<NullFieldsSearchReply, NullFieldsError>) -> Void)
}
class NullFieldsFlutterApi: NullFieldsFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullFieldsPigeonCodec {
    return NullFieldsPigeonCodec.shared
  }
  func search(
    request requestArg: NullFieldsSearchRequest,
    completion: @escaping (Result<NullFieldsSearchReply, NullFieldsError>) -> Void
  ) {
    let channelName: String =
      "dev.flutter.pigeon.pigeon_integration_tests.NullFieldsFlutterApi.search\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(
      name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([requestArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(NullFieldsError(code: code, message: message, details: details)))
      } else if listResponse[0] == nil {
        completion(
          .failure(
            NullFieldsError(
              code: "null-error",
              message: "Flutter api returned null value for non-null return value.", details: "")))
      } else {
        let result = listResponse[0] as! NullFieldsSearchReply
        completion(.success(result))
      }
    }
  }
}
