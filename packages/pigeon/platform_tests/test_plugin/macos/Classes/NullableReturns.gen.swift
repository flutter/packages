// Copyright 2013 The Flutter Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
//
// Autogenerated from Pigeon, do not edit directly.
// See also: https://pub.dev/packages/pigeon
// swift-format-ignore-file: AlwaysUseLowerCamelCase

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class NullableReturnsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "NullableReturnsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? NullableReturnsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> NullableReturnsError {
  return NullableReturnsError(
    code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.",
    details: "")
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

private class NullableReturnsPigeonCodecReader: FlutterStandardReader {
}

private class NullableReturnsPigeonCodecWriter: FlutterStandardWriter {
}

private class NullableReturnsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return NullableReturnsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return NullableReturnsPigeonCodecWriter(data: data)
  }
}

class NullableReturnsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = NullableReturnsPigeonCodec(
    readerWriter: NullableReturnsPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullableReturnHostApi {
  func doit() throws -> Int64?
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullableReturnHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullableReturnsPigeonCodec.shared }
  /// Sets up an instance of `NullableReturnHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullableReturnHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let doitChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.pigeon_integration_tests.NullableReturnHostApi.doit\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      doitChannel.setMessageHandler { _, reply in
        do {
          let result = try api.doit()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      doitChannel.setMessageHandler(nil)
    }
  }
}
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullableReturnFlutterApiProtocol {
  func doit(completion: @escaping (Result<Int64?, NullableReturnsError>) -> Void)
}
class NullableReturnFlutterApi: NullableReturnFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullableReturnsPigeonCodec {
    return NullableReturnsPigeonCodec.shared
  }
  func doit(completion: @escaping (Result<Int64?, NullableReturnsError>) -> Void) {
    let channelName: String =
      "dev.flutter.pigeon.pigeon_integration_tests.NullableReturnFlutterApi.doit\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(
      name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage(nil) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(NullableReturnsError(code: code, message: message, details: details)))
      } else {
        let result: Int64? = nilOrValue(listResponse[0])
        completion(.success(result))
      }
    }
  }
}
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullableArgHostApi {
  func doit(x: Int64?) throws -> Int64
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullableArgHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullableReturnsPigeonCodec.shared }
  /// Sets up an instance of `NullableArgHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullableArgHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let doitChannel = FlutterBasicMessageChannel(
      name: "dev.flutter.pigeon.pigeon_integration_tests.NullableArgHostApi.doit\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      doitChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let xArg: Int64? = nilOrValue(args[0])
        do {
          let result = try api.doit(x: xArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      doitChannel.setMessageHandler(nil)
    }
  }
}
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullableArgFlutterApiProtocol {
  func doit(x xArg: Int64?, completion: @escaping (Result<Int64?, NullableReturnsError>) -> Void)
}
class NullableArgFlutterApi: NullableArgFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullableReturnsPigeonCodec {
    return NullableReturnsPigeonCodec.shared
  }
  func doit(x xArg: Int64?, completion: @escaping (Result<Int64?, NullableReturnsError>) -> Void) {
    let channelName: String =
      "dev.flutter.pigeon.pigeon_integration_tests.NullableArgFlutterApi.doit\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(
      name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([xArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(NullableReturnsError(code: code, message: message, details: details)))
      } else {
        let result: Int64? = nilOrValue(listResponse[0])
        completion(.success(result))
      }
    }
  }
}
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullableCollectionReturnHostApi {
  func doit() throws -> [String?]?
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullableCollectionReturnHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullableReturnsPigeonCodec.shared }
  /// Sets up an instance of `NullableCollectionReturnHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullableCollectionReturnHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let doitChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.pigeon_integration_tests.NullableCollectionReturnHostApi.doit\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      doitChannel.setMessageHandler { _, reply in
        do {
          let result = try api.doit()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      doitChannel.setMessageHandler(nil)
    }
  }
}
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullableCollectionReturnFlutterApiProtocol {
  func doit(completion: @escaping (Result<[String?]?, NullableReturnsError>) -> Void)
}
class NullableCollectionReturnFlutterApi: NullableCollectionReturnFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullableReturnsPigeonCodec {
    return NullableReturnsPigeonCodec.shared
  }
  func doit(completion: @escaping (Result<[String?]?, NullableReturnsError>) -> Void) {
    let channelName: String =
      "dev.flutter.pigeon.pigeon_integration_tests.NullableCollectionReturnFlutterApi.doit\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(
      name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage(nil) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(NullableReturnsError(code: code, message: message, details: details)))
      } else {
        let result: [String?]? = nilOrValue(listResponse[0])
        completion(.success(result))
      }
    }
  }
}
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol NullableCollectionArgHostApi {
  func doit(x: [String?]?) throws -> [String?]
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class NullableCollectionArgHostApiSetup {
  static var codec: FlutterStandardMessageCodec { NullableReturnsPigeonCodec.shared }
  /// Sets up an instance of `NullableCollectionArgHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(
    binaryMessenger: FlutterBinaryMessenger, api: NullableCollectionArgHostApi?,
    messageChannelSuffix: String = ""
  ) {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let doitChannel = FlutterBasicMessageChannel(
      name:
        "dev.flutter.pigeon.pigeon_integration_tests.NullableCollectionArgHostApi.doit\(channelSuffix)",
      binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      doitChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let xArg: [String?]? = nilOrValue(args[0])
        do {
          let result = try api.doit(x: xArg)
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      doitChannel.setMessageHandler(nil)
    }
  }
}
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol NullableCollectionArgFlutterApiProtocol {
  func doit(
    x xArg: [String?]?, completion: @escaping (Result<[String?], NullableReturnsError>) -> Void)
}
class NullableCollectionArgFlutterApi: NullableCollectionArgFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: NullableReturnsPigeonCodec {
    return NullableReturnsPigeonCodec.shared
  }
  func doit(
    x xArg: [String?]?, completion: @escaping (Result<[String?], NullableReturnsError>) -> Void
  ) {
    let channelName: String =
      "dev.flutter.pigeon.pigeon_integration_tests.NullableCollectionArgFlutterApi.doit\(messageChannelSuffix)"
    let channel = FlutterBasicMessageChannel(
      name: channelName, binaryMessenger: binaryMessenger, codec: codec)
    channel.sendMessage([xArg] as [Any?]) { response in
      guard let listResponse = response as? [Any?] else {
        completion(.failure(createConnectionError(withChannelName: channelName)))
        return
      }
      if listResponse.count > 1 {
        let code: String = listResponse[0] as! String
        let message: String? = nilOrValue(listResponse[1])
        let details: String? = nilOrValue(listResponse[2])
        completion(.failure(NullableReturnsError(code: code, message: message, details: details)))
      } else if listResponse[0] == nil {
        completion(
          .failure(
            NullableReturnsError(
              code: "null-error",
              message: "Flutter api returned null value for non-null return value.", details: "")))
      } else {
        let result = listResponse[0] as! [String?]
        completion(.success(result))
      }
    }
  }
}
