import FlutterMacOS
import Cocoa
import XCTest

@testable import alternate_language_test_plugin

class RunnerTests: XCTestCase {
  // Currently there are no macOS Objective-C unit tests, since the expectation
  // is that the iOS Objective-C unit tests provide sufficient native unit test
  // coverage. If macOS-specific issue come up, they can be tested here.
}
