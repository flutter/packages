name: web_pointer_interceptor
description: A new Flutter package project.
version: 0.8.0
repository: https://github.com/flutter/packages

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
