name: dart_hooks
description: Hooks for camera_android_camerax package
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '^3.11.0'

dependencies:
  path: ^1.8.0

dev_dependencies:
  mockito: ^5.4.0
  test: ^1.24.0
