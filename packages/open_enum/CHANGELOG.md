## 0.1.0

* Initial release of `open_enum`.
* Introduce the `OpenEnum` extension type base.
* Add generic `byValue` extension helper for `Iterable` collections of `OpenEnum`.
* Add specialized `byName` and `byNameOrNull` string-based lookup helper extensions.
* Provide documentation, comprehensive unit tests, and usage examples.
