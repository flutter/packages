## 0.0.2

* Refactor `CircleAvatar` to use background and foreground `DecorationImage`
* Deprecate `backgroundImage`, `onBackgroundImageError`, `foregroundImage`, and `onForegroundImageError` in `CircleAvatar`. Use `backgroundDecorationImage` and `foregroundDecorationImage` instead.
* Updates minimum supported SDK version to Flutter 3.38/Dart 3.10.

## 0.0.1

* Initial setup of the `material_ui` package, preparing for decoupling Material widgets from the Flutter framework.
