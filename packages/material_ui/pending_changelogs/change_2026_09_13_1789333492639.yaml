changelog: |
  - Introduce `activeIndex` property to `CarouselController` and `CarouselPosition`.
  - Update `CarouselView.weighted` `onIndexChanged` to report the index of the maximum weight item based on `activeIndex`.
version: minor
