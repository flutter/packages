changelog: |
  - Adds `CarouselController.activeIndex` for retrieving the currently active carousel item.
  - Update `onIndexChanged` callback to rely on `activeIndex` for both weighted and unweighted `CarouselView`s.
  - Update documentation to clearly distinguish `activeIndex` from `leadingItem`.
version: minor
