changelog: |
  - Uses `ThemeData.fallbackScrimColor` (`ColorScheme.scrim` at 32% opacity in Material 3, `Colors.black54` in Material 2) as the default fallback for dialog, modal bottom sheet, and drawer barrier/scrim colors.
version: patch
