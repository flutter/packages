changelog: |
  - Adds `ThemeData.fallbackScrimColor` and uses it as the default fallback for dialog, modal bottom sheet, and drawer barrier/scrim colors. Material 3 uses `ColorScheme.scrim` at 32% opacity (changing the default opacity from 54%), and Material 2 continues to use `Colors.black54`.
version: minor
