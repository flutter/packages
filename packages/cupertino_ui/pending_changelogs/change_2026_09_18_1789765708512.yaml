changelog: |
  - Removes the `awaitNotRequired` annotations from `showCupertinoModalPopup` and `showCupertinoDialog`, which require Flutter 3.47 or later, to restore compatibility with the declared minimum Flutter version.
version: patch
