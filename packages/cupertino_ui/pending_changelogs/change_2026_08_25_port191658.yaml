changelog: |
  - Fix `CupertinoTextFormFieldRow` validators sometimes seeing a stale value when `FormState.validate()` runs from a `TextEditingController` listener registered before the field's own listener.
version: patch
